import org.junit.Test;
import static org.junit.Assert.*;

public class MainTest {
    @Test public void testSomeLibraryMethod() {
        assertTrue("some method should return 'true'", true);
    }
}
